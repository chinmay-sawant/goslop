# Go bad-practice (BP) detectors

Skeleton for Phase 8. Ruleset: `ruleset/golang/bad-practices.json`.

No BP detectors are registered in the seed set. Recommended profile keeps BP off.

Remaining work: port Rust modules under `codehound/src/lang/go/detectors/bad_practices/rules/` (~39 rule modules, ~135 rules).
