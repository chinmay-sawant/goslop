# goslop text fixture — fromisoformat ValueError:pass continues without defined fallback (WeThePeople-style TP)
lang: python
file: services/dates.py
---
def parse_maybe(value):
    try:
        date = datetime.fromisoformat(value)
    except ValueError:
        pass
    process(date)
