# goslop text fixture
lang: python
file: BP-PY-12-builtins-exec-safe.py
---
from sqlmodel import Session, select

import js


def load_one(session: Session, model):
    return session.exec(select(model)).first()


def run_qt(app):
    return app.exec()


def run_js_bridge(code):
    return js.window.exec(code)
