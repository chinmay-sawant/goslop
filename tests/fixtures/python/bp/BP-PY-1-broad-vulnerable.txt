# goslop text fixture — bare re-raise-only handler still hides nothing but stays
# reportable: a generic catch is the weakness itself (WeThePeople tracing.py:76 TP)
lang: python
file: broad.py
---
def f():
    try:
        x()
    except Exception:
        raise
