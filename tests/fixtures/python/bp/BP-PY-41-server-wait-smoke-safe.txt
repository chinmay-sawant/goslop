# goslop text fixture — niquests test_basic_waiting_server: socket + Event
# infrastructure smoke, not a placeholder product test.
lang: python
file: tests/test_testserver.py
---
import socket
import threading
import time

from tests.testserver.server import Server


def test_basic_waiting_server():
    """the server waits for the block_server event to be set before closing"""
    block_server = threading.Event()

    with Server.basic_response_server(wait_to_close_event=block_server) as (
        host,
        port,
    ):
        sock = socket.socket()
        sock.connect((host, port))
        sock.sendall(b"send something")
        time.sleep(2.5)
        sock.sendall(b"still alive")
        block_server.set()
