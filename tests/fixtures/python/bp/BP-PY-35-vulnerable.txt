# Fixture for BP-PY-35 (vulnerable)
lang: python
file: BP-PY-35-vulnerable.py
---
from sqlalchemy import text

def q(conn, name):
    return conn.execute(text(f"SELECT * FROM t WHERE name = '{name}'"))
