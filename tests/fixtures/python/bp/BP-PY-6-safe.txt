# goslop text fixture
lang: python
file: lib.py
---
def check_user(user):
    if user is None:
        raise ValueError("missing user")
    if not user.is_active:
        raise ValueError("inactive")
    return user
