# goslop text fixture
lang: python
file: app.py
---
from fastapi import FastAPI, Depends
app = FastAPI()
STORE = {}

@app.get("/x")
def read_x(k: str):
    global STORE
    STORE[k] = 1
    return STORE
