# goslop text fixture — FileResponse of path built from module constants only (no route param)
lang: python
file: api/public_artifacts_routes.py
---
from fastapi import APIRouter, HTTPException
from fastapi.responses import FileResponse

router = APIRouter()
PUBLIC_ARTIFACTS_DIR = Path("data/public")


@router.get("/artifacts/dashboard")
async def get_authority_dashboard_artifact():
    path = PUBLIC_ARTIFACTS_DIR / "authority_dashboard.json"
    if not path.exists():
        raise HTTPException(status_code=404, detail="Dashboard artifact not generated yet")
    return FileResponse(path, media_type="application/json", filename=path.name)
