# Fixture for BP-PY-6 (vulnerable)
lang: python
file: BP-PY-6-vulnerable.py
---
def check_user(request):
    assert request.user.is_authenticated
    assert request.path.startswith("/admin")
    return request.user
