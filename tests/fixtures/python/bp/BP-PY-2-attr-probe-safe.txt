# goslop text fixture — AttributeError after pure super().__getattribute__
# probe (niquests models.py:_gather FP)
lang: python
file: attr_probe.py
---
class Response:
    def _gather(self) -> None:
        try:
            super().__getattribute__("_promise")
            super().__getattribute__("connection").gather(self)
        except AttributeError:
            pass
