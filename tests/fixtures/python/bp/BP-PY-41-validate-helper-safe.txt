# Fixture for BP-PY-41 (safe) — validate_* raises on failure (assertion by exception)
lang: python
file: tests/unit/trust/test_temporal_trust_tools.py
---
from pathlib import Path

from tools.validate_schemas import validate_schema_file

ROOT = Path(__file__).resolve().parents[3]


def test_trust_schemas_parse_and_validate_examples():
    for relative in [
        "schemas/signature.schema.json",
        "schemas/transparency-log-entry.schema.json",
    ]:
        validate_schema_file(ROOT / relative)
