# Fixture for BP-PY-41 (vulnerable) — side-effect-only test, no asserts
lang: python
file: test_login_helper_vuln.py
---
def test_login():
    client.post("/login", json={"u": "a"})
    save_fixture("out.pdf")
