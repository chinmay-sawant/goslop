# goslop text fixture — cleanup-then-propagate handler; the failure is re-raised
# after resource cleanup (niquests utils.py:256, safer __init__.py:312 FP shape)
lang: python
file: broad.py
---
def f():
    try:
        x()
    except Exception:
        cleanup()
        raise
