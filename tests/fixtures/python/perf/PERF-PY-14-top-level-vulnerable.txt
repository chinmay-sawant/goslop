# PERF-PY-14 top-level variant (vulnerable)
lang: python
file: events.py
---
def create(key):
    existing = Event.objects.filter(idempotency_key=key).first()
    if existing:
        return existing
    return Event.objects.create(idempotency_key=key)
