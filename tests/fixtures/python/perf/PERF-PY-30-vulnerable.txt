# Fixture for PERF-PY-30 (vulnerable)
lang: python
file: app/format.py
---
def format_grid(rows):
    out = b""
    for row in rows:
        for cell in row:
            out = out + b"<c>"
            out = out + cell.encode()
            out = out + b"</c>"
    return out
