# PERF-PY-19 sqlalchemy unbounded lock (vulnerable)
lang: python
file: sweeper.py
---
def release(session):
    with session.begin():
        for row in session.query(Reservation).with_for_update():
            row.release()
