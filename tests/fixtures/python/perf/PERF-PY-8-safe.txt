lang: python
file: delivery.py
---
def deliver(session):
    items = session.query(DeliveryOutbox).options(joinedload(DeliveryOutbox.author)).all()
    for item in items:
        send(item.author.url)
