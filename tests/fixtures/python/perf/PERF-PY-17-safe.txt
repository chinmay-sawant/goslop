# Fixture for PERF-PY-17 (safe)
lang: python
file: config/database.py
---
ENVIRONMENT = "production"
DATABASE_URL = "postgresql://db/service"
engine = create_engine(DATABASE_URL, pool_pre_ping=True, pool_recycle=1800, pool_timeout=10)
