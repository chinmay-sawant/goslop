# goslop text fixture
lang: python
file: CWE-89-orm-expression-safe.py
---
async def find(session, owner_id):
    stmt = (
        select(Item)
        .where(Item.owner_id == owner_id)
    )
    return await session.execute(stmt)
