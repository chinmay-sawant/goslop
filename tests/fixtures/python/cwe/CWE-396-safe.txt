# goslop text fixture (.txt) — materialized to .py at test/runtime
lang: python
file: CWE-396-safe.py
---
def load_settings(path):
    try:
        return open(path).read()
    except OSError:
        return "defaults"
