# goslop text fixture — logger.exception then bare return 1 stays reportable
# (WeThePeople jobs audited TPs; not an exit-code named constant)
lang: python
file: jobs/sync_data.py
---
import logging
logger = logging.getLogger(__name__)

def main():
    try:
        run_sync()
        return 0
    except Exception:
        logger.exception("sync failed")
        return 1
