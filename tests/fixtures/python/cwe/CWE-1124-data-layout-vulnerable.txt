# goslop text fixture
lang: python
file: CWE-1124-data-layout-vulnerable.py
---
if a:
    if b:
        if c:
            if d:
                if e:
                    if f:
                        action()
