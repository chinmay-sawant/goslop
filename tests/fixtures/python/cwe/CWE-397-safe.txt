# goslop text fixture (.txt) — materialized to .py at test/runtime
lang: python
file: CWE-397-safe.py
---
def require_account(account):
    if account is None:
        raise ValueError("account is required")
