# goslop text fixture (.txt) — materialized to .py at test/runtime
lang: python
file: CWE-914-safe.py
---
"""CWE-914 safe: request value selects only from a fixed allowlist."""


def resolve_value(request):
    allowed = {"home": "HOME_PAGE", "help": "HELP_PAGE"}
    return allowed[request.args["name"]]
