# goslop text fixture (.txt) — materialized to .py at test/runtime
lang: python
file: CWE-770-safe.py
---
from flask import Flask, request

app = Flask(__name__)
app.config["MAX_CONTENT_LENGTH"] = 1024 * 1024

def receive():
    return request.get_data(cache=True)
