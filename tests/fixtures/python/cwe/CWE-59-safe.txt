# goslop text fixture (.txt) — materialized to .py at test/runtime
lang: python
file: CWE-59-safe.py
---
import os

fd = os.open(path, os.O_RDONLY | os.O_NOFOLLOW)

if not os.path.islink(checked_path):
    open(other_path)
