# goslop text fixture — offline verify runner under scripts/parva_*_verify.py
# (Project_Parva scripts/parva_rulelang_verify.py path FP shape)
lang: python
file: scripts/parva_rulelang_verify.py
---
def main():
    try:
        run_checks()
        return 0
    except Exception as exc:
        print(f"verification failed: {exc}")
        return 1
