# goslop text fixture — same-type re-raise of the caught exception stays
# reportable (WeThePeople middleware/tracing.py:76 TP shape)
lang: python
file: CWE-396-batch3-reraise-from-vulnerable.py
---
class DomainError(Exception):
    pass

def load_pipeline(name):
    try:
        return build(name)
    except Exception as exc:
        raise exc
