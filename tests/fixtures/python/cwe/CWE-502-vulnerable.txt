# goslop text fixture (.txt) — materialized to .py at test/runtime
lang: python
file: CWE-502-vulnerable.py
---
"""CWE-502 vulnerable: pickle.loads and yaml.load without SafeLoader."""

import pickle
import yaml


def load_blob(data: bytes):
    return pickle.loads(data)


def load_yaml(data: str):
    return yaml.load(data)
