# goslop text fixture (.txt) — materialized to .py at test/runtime
lang: python
file: CWE-459-safe.py
---
import os
import tempfile

def stage_upload():
    descriptor, path = tempfile.mkstemp()
    os.close(descriptor)
    os.unlink(path)
