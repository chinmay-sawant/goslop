# goslop text fixture (.txt) — materialized to .py at test/runtime
lang: python
file: CWE-605-vulnerable.py
---
import socket


def serve():
    sock = socket.socket()
    sock.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
    sock.bind(("0.0.0.0", 8080))
