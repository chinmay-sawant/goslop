# goslop text fixture — generic except hides failure without recording it
lang: python
file: CWE-396-batch3-result-error-vulnerable.py
---
def run_scenario(result, fn):
    try:
        fn()
        result.ok = True
    except Exception:
        result.ok = False
