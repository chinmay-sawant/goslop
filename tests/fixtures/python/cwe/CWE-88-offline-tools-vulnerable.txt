# goslop text fixture — CWE-88 free operand after flag in app code (TP shape)
lang: python
file: app/extract.py
---
import subprocess
from pathlib import Path


def extract_pdftotext(pdf_path: Path) -> str:
    result = subprocess.run(
        ["pdftotext", "-layout", str(pdf_path), "-"],
        capture_output=True,
        text=True,
        check=False,
    )
    return result.stdout
