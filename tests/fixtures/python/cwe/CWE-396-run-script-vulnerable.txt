# goslop text fixture — same body outside scripts/run_* stays reportable
lang: python
file: services/journeys.py
---
def main():
    try:
        return run_journeys()
    except Exception as exc:
        _write_report(status="failed", error=str(exc))
        raise
