# goslop text fixture
lang: python
file: CWE-1124-declaration-scope-vulnerable.py
---
if a:
    if b:
        if c:
            if d:
                if e:
                    if f:
                        action()
