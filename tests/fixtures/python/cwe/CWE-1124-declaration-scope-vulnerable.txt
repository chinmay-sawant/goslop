# goslop text fixture
lang: python
file: CWE-1124-declaration-scope-vulnerable.py
---
def deeply_nested(a, b, c, d, e, f):
    if a:
        if b:
            if c:
                if d:
                    if e:
                        if f:
                            result = 1
