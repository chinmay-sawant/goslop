# goslop text fixture (.txt) — materialized to .py at test/runtime
lang: python
file: CWE-565-safe.py
---
def account(request):
    user_id = request.cookies.get("user_id")
    if not verify_session(user_id):
        return None
    return load_user(user_id)
