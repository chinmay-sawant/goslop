# goslop text fixture
lang: python
file: CWE-89-static-constant-vulnerable.py
---
import sqlite3


def find_user(db: str, user_id: int):
    with sqlite3.connect(database=db) as conn:
        conn.execute(f"SELECT * FROM users WHERE id = {user_id}")
