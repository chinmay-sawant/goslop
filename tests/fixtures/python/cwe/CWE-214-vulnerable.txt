# goslop text fixture (.txt) — materialized to .py at test/runtime
lang: python
file: CWE-214-vulnerable.py
---
import subprocess


def connect(password):
    subprocess.run(["client", "--password", password])
