# goslop text fixture — wrap-raise after a JS-bridge try body stays reportable
# (must not silence voicetag-style translation of the failure)
lang: python
file: pyodide_bridge_wrap_raise.py
---
def send(js_response, url):
    try:
        return run_sync(js_response.bytes())
    except Exception as e:
        raise ConnectionError(f"Failed to fetch {url}: {e}") from e
