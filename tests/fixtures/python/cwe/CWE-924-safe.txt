# goslop text fixture (.txt) — materialized to .py at test/runtime
lang: python
file: CWE-924-safe.py
---
import hmac


def billing_webhook(request):
    body = request.get_data()
    if not hmac.compare_digest(request.headers["X-Signature"], sign(body)):
        return "invalid", 400
    process_event(body)
