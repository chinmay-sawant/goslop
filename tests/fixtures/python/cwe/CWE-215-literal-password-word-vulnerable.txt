# goslop text fixture — sensitive identifier in debug args (vulnerable)
lang: python
file: CWE-215-literal-password-word-vulnerable.py
---
import logging


def trace(password):
    logging.debug("auth failed", password)
