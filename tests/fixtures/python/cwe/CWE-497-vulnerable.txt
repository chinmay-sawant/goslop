# goslop text fixture (.txt) — materialized to .py at test/runtime
lang: python
file: CWE-497-vulnerable.py
---
import os
from flask import jsonify


def diagnostics():
    return jsonify(environment=dict(os.environ))
