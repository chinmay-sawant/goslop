# goslop text fixture — empty pass handler after non-conversion parse continues
lang: python
file: services/dates.py
---
def parse_maybe(value):
    try:
        date = datetime.fromisoformat(value)
    except ValueError:
        pass
    process(date)
