# CWE-778: Insufficient Logging (pure stdlib)
lang: go
file: CWE-778-vulnerable.pure.go
variant: pure-go
---
package sample

import (
	"database/sql"
	"net/http"
)

// Vulnerable: failed authentication attempts are not recorded.
// Operators cannot detect credential stuffing without audit events.
func SignIn(w http.ResponseWriter, r *http.Request, db *sql.DB) {
	user := r.FormValue("username")
	pass := r.FormValue("password")
	var stored string
	err := db.QueryRow("SELECT password FROM users WHERE username = $1", user).Scan(&stored)
	if err != nil || stored != pass {
		http.Error(w, "unauthorized", http.StatusUnauthorized)
		return
	}
	w.WriteHeader(http.StatusOK)
}
