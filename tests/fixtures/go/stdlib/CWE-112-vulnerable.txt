# CWE-112 positive: untrusted XML parsed without schema validation (pure stdlib)
lang: go
file: CWE-112-vulnerable.pure.go
variant: pure-go
---
package sample

import (
	"encoding/json"
	"encoding/xml"
	"io"
	"net/http"
)

// CWE-112 Missing XML Validation
// Vulnerable line: Unmarshal accepts arbitrary client XML.
func HandleManifest(w http.ResponseWriter, r *http.Request) {
	payload, err := io.ReadAll(r.Body)
	if err != nil {
		http.Error(w, "body", http.StatusBadRequest)
		return
	}
	var manifest struct {
		SKU     string  `xml:"sku"`
		Price   float64 `xml:"price"`
	}
	if err := xml.Unmarshal(payload, &manifest); err != nil {
		http.Error(w, "xml", http.StatusBadRequest)
		return
	}
	w.Header().Set("Content-Type", "application/json")
	w.WriteHeader(http.StatusOK)
	_ = json.NewEncoder(w).Encode(map[string]any{"sku": manifest.SKU, "price": manifest.Price})
}
