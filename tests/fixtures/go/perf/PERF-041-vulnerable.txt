# PERF-041 positive: log.Println used inside a request handler
lang: go
file: PERF-041-vulnerable.go
---
package sample

import (
	"log"
	"net/http"

	"github.com/gin-gonic/gin"
)

func Handler(c *gin.Context) {
	log.Println("hit handler")
	c.JSON(http.StatusOK, gin.H{"ok": true})
}
