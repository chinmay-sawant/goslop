# PERF-015 positive: strconv.Itoa inside loop
lang: go
file: PERF-015-vulnerable.go
---
package sample

import "strconv"

func NumbersToStrings(values []int) []string {
	out := make([]string, 0, len(values))
	for _, v := range values {
		out = append(out, strconv.Itoa(v))
	}
	return out
}
