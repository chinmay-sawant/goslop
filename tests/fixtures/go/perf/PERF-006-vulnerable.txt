# PERF-006 positive: fmt.Sprintf inside loop
lang: go
file: PERF-006-vulnerable.go
---
package sample

import "fmt"

func FormatAll(values []int) []string {
	out := make([]string, 0, len(values))
	for _, v := range values {
		out = append(out, fmt.Sprintf("value=%d", v))
	}
	return out
}
