# PERF-114-interface positive: pure same-type loop copy still fires
lang: go
file: PERF-114-interface-vulnerable.go
variant: stdlib
---
package sample

import "fmt"

func Copy(src []int) []int {
	dst := make([]int, len(src))
	for i, v := range src {
		dst[i] = v
	}
	return dst
}

func _() {
	out := Copy([]int{1, 2, 3})
	fmt.Println(out)
}
