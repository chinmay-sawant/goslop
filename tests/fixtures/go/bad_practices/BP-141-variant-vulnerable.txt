# BP-141 positive variant: a local composite value reaches NamedExec without tags.
lang: go
file: BP-141-variant-vulnerable.go
variant: sqlx-named-composite
---
package sample

import (
	"fmt"

	"github.com/jmoiron/sqlx"
)

type Account struct {
	AccountID int
	Email     string
}

func insertAccount(db *sqlx.DB) error {
	payload := &Account{AccountID: 7, Email: "ops@example"}
	_, err := db.NamedExec(`INSERT INTO accounts (account_id, email) VALUES (:account_id, :email)`, payload)
	return fmt.Errorf("insert account: %w", err)
}
